# Mock API Server for bloodBankUI

This is a simple Express-based mock API implementing the endpoints the Angular frontend expects. It uses in-memory data and is intended for local development.

Run:

```
cd server
npm install
npm run start
```

Server runs on port 8090 by default to match `src/environments/environment.ts`.
